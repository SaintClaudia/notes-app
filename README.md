# notes-app
A minimal, dark-themed notes app with tap-to-complete checklists, tags, archive, and a dashboard summary that's instant and local by default, quietly upgraded by an AI-generated summary (via a backend proxy — see `worker/`) when available.
